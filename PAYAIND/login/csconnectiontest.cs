namespace PAYAIND
{
    public class csconnectiontest
    {
        public bool testConnection()
        {
           
            //try
            //{
            //    string con =PAYAIND.Properties.Settings.Default.payazobnetConnectionString;
            //    using (var connection = new System.Data.OleDb.OleDbConnection("Provider=SQLOLEDB; " + con))
            //    {
            //        using (Ping ping1 = new Ping())
            //        {
            //           ping.microsoft Pingip = new ping.microsoft();
            //            if (!Pingip.microsoftping("Provider=SQLOLEDB; " + con))
            //                return false;
            //            PingReply replyping = ping1.Send(IPAddress.Parse(connection.DataSource), 2000);
            //            if (replyping.Status == IPStatus.Success)
                            return true;
                //        else
                //         return false;
                //    }
            //    //}
            //}
            //catch (Exception)
            //{
            //    return false;
            //}

        }
    }
}




       
    